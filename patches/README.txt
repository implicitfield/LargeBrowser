Third-party Patches

The naming convention for patches in this directory is [project]-[index]-[name].

Details:
SafariConverterLib:
0001-output-json.patch: Outputs generated JSON to stderr.

WebKit:
0001-ContentBlockers-add-execute-script-action.patch: Adds an
'execute-script' rule to Content Blockers, which works with a
user-provided payload.
